Macnotifier is an utility permitting to start automatically programs or scripts associated to  specific MAC Addresses or company IDs( as they are registered in the IEEE registers containing the MAC adresses blocks assignees). when they are notified to the system running this program. Some other utilities are provided ( MAC Lookup tool, some small scripts to convert the IEEE DBs, etc). 
